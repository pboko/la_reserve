<%= content_for(:title) do %>
  Home
<% end %>

<%= content_for(:description) do %>
  <%# TODO %>
<% end %>

<div class="homepage-hero-module">
    <div class="video-container">
      <div class="container" data-sr>
        <div class="slogan">
          <h1>Cocoonez vos réservations</h1>
          <p>Vos réservations sont votre business.<br>Donnez leur l'amour qu'elles méritent.</p>
        </div>
      </div>
        <div class="filter"></div>
        <video autoplay loop class="fillWidth">
            <source src=<%= image_url('repas.mp4') %> type="video/mp4" />
        <div class="poster hidden">
          <%= image_tag "tabledressee.jpg" %>
        </div>
    </div>
</div>





<!-- Push configuration to Heroku
 # View current environment variables
 $ heroku config
 # Add environment variables from `config/application.yml`
 $ figaro heroku:set -e production
 # Check that again!
 $ heroku config -->
