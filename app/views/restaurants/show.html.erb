<div class="margin-top">
  <div class="container">
    <div class="row">
      <div class="col-md-10 col-md-offset-2">

        <div class="text-center" id="focus-day">
          <%= link_to :date => previous_day(@date) do  %>
            <i class="fa fa-arrow-left"></i>
          <% end %>
          <h2 class= "text-center"><%= @date.strftime("%A %d %B %Y") %> </h2>
          <%= link_to :date => next_day(@date) do  %>
            <i class="fa fa-arrow-right"></i>
          <% end %>
        </div>

        <div class="accueil">
            <h3> Midi </h3>

            <p> <%= total_cuttlery_for(:midi, @date, @restaurant.bookings) %> couverts </p>
            <p> Liste d'attente : </p>
        </div>

        <div class="accueil">
          <h3> Soir </h3>
          <p> <%= total_cuttlery_for(:soir, @date, @restaurant.bookings) %> couverts </p>
          <p> Liste d'attente : </p>
        </div>

        <div class="accueil">
          <h3> Liste d'attente </h3>
          <p>  N réservations </p>
        </div>

        <div class="accueil">
          <h3> Post-it </h3>
        </div>

        <div class="accueil">
          <h3> VIP midi </h3>
          <% bookings.for_period(:midi).for_date(date).each do |booking| %>
          <% if booking.customer.satus == "VIP" %>

          <p> <%= booking.customer.first_name %> </p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
