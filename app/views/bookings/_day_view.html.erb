<div class="row" id="dayview">

  <h2 id="date-detail"><%= l (Date.today), format: :long %> </h2>

      <ul class="nav nav-pills text-center">
        <li role="presentation" class="active switch-button"><a href="#resa-midi" data-toggle="tab">Midi</a></li>
        <li role="presentation" class="switch-button"><a href="#resa-soir" data-toggle="tab">Soir</a></li>
        <li role="presentation" class="switch-button"><a href="#liste-dattente" data-toggle="tab">Liste d'attente</a></li>
      </ul>

  <div class="row">
    <div class="col-md-11">
      <div class="panel panel-default">
          <div class="tab-content">
            <div role="tabpanel" class="tab-pane active" id="resa-midi">
              <table class="table">
                <thead>
                  <tr>
                    <th><i class="fa fa-clock-o"></i></th>
                    <th>Statut</th>
                    <th><i class="fa fa-cutlery"></i></th>
                    <th><i class="fa fa-user"></i></th>
                    <th>Notes</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody class="text-center">
                  <% bookings.for_period(:midi).for_date(date).each do |booking| %>
                    <div <% unless booking.waiting_list? %>>
                      <tr>
                        <td>
                          <%= booking.start_time.strftime("%H:%M")%>
                        </td>
                        <td>
                          <%= booking.status %>
                        </td>
                        <td>
                          <%= booking.pax %>
                        </td>
                         <td>
                        <%= booking.customer.last_name if booking.customer.present? %>
                         </td>
                        <td>
                          <%= booking.notes %>
                        </td>
                        <td class="index-icons">
                          <%= link_to edit_restaurant_booking_path(restaurant, booking) do %>
                            <i class="fa fa-pencil"></i>
                          <%end %>
                          <%= link_to restaurant_booking_path(restaurant, booking),
                          method: :delete,
                          data: { confirm: "Are you sure?" } do %>
                            <i class="fa fa-trash-o"></i>
                            <% end %>
                        </td>
                      </tr>
                    <% end %>
                    </div>
                  <% end %>
                </tbody>
              </table>
              <div class="total">
                <h3>Total : <%= lunch_booking_total %> couverts </h3>
              </div>
            </div>

            <div role="tabpanel" class="tab-pane" id="resa-soir">
              <table class="table">
                <thead>
                  <tr>
                    <th><i class="fa fa-clock-o"></i></th>
                    <th>Statut</th>
                    <th><i class="fa fa-cutlery"></i></th>
                    <th><i class="fa fa-user"></i></th>
                    <th>Notes</th>
                    <th></th>
                  </tr>
                </thead>
                <tbody class="text-center">
                  <% bookings.for_period(:soir).for_date(date).each do |booking| %>
                  <div <% unless booking.waiting_list? %>>
                    <tr>
                      <td>
                        <%= booking.start_time.strftime("%H:%M")%>
                      </td>
                      <td>
                        <%= booking.status %>
                      </td>
                      <td>
                        <%= booking.pax %>
                      </td>
                       <td>
                      <%= booking.customer.last_name if booking.customer.present?%>
                       </td>
                      <td>
                        <%= booking.notes %>
                      </td>
                      <td class="index-icons">
                        <%= link_to edit_restaurant_booking_path(restaurant, booking) do %>
                          <i class="fa fa-pencil"></i>
                        <%end %>
                        <%= link_to restaurant_booking_path(restaurant, booking),
                        method: :delete,
                        data: { confirm: "Are you sure?" } do %>
                          <i class="fa fa-trash-o"></i>
                        <% end %>
                      </td>
                    </tr>
                    <% end %>
                  </div>
                <% end %>
                </tbody>
              </table>
              <div class="total">
                <h3>Total : <%= diner_booking_total %> couverts </h3>
              </div>
            </div>

            <div role="tabpanel" class="tab-pane" id="liste-dattente">
              <table class="table">
                <thead>
                  <tr>
                    <th><i class="fa fa-clock-o"></i></th>
                    <th>Service</th>
                    <th><i class="fa fa-cutlery"></i></th>
                    <th><i class="fa fa-user"></i></th>
                    <th></th>
                  </tr>
                </thead>
                <tbody class="text-center">
                  <% bookings.for_date(date).each do |booking| %>
                    <div <% if booking.waiting_list? %>>
                      <tr>
                        <td>
                          <%= booking.start_time.hour %>h<%= booking.start_time.min %>
                        </td>
                         <td>
                          <%= booking.period %>
                        </td>
                        <td>
                          <%= booking.pax %>
                        </td>
                        <td>
                          <%= booking.customer.last_name if booking.customer.present? %>
                        </td>
                        <td class="index-icons">
                          <%= link_to edit_restaurant_booking_path(restaurant, booking) do %>
                            <i class="fa fa-pencil"></i>
                          <%end %>
                          <%= link_to restaurant_booking_path(restaurant, booking),
                          method: :delete,
                          data: { confirm: "Are you sure?" } do %>
                            <i class="fa fa-trash-o"></i>
                            <% end %>
                        </td>
                      </tr>
                    <% end %>
                    </div>
                  <% end %>
                </tbody>
              </table>
              <div class="total">
                <h3>Total : <%= total_waiting_cuttlery_for(:midi, date, @bookings) + total_waiting_cuttlery_for(:soir, date, @bookings) %> couverts </h3>
              </div>
            </div>
          </div>
      </div>
    </div>
  </div>
</div>

