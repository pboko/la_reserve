$('#dayview').replaceWith("<%= j render 'bookings/day_view', date: @date, restaurant: @restaurant, bookings: @bookings, diner_booking_total: @diner_booking_total, lunch_booking_total: @lunch_booking_total %>");
//window.location.search = '<%= "date=#{@date}" %>'

window.location.href = "#";

if ("<%= @time %>" == 'midi') {
  window.location.href = "#resa-midi";
} else if ("<%= @time %>" == 'soir') {
  window.location.href = "#resa-soir";
}
// permet de rediriger vers le midi ou le soir en fonction du params.

$('body').scrollTop(0)

  // $('body').animate({
  //  scrollTop: $('#dayview').offset().top - 70
  // }, 1000);

$('#dayview .datepicker').datepicker({
  format: "yyyy-mm-dd",
  language: "fr",
  startView: new Date(),
  weekStart: 1,
  orientation: "top auto",
  autoclose: true
});
