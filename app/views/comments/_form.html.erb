<%= simple_form_for([ restaurant, comment ], remote: true) do |f| %>
  <%= f.input :service_date, as: :hidden, input_html: { value: service_date } %>
  <%= f.input :content, as: :string, placeholder: "ajouter un commentaire" , label: false, class: "form-control" %>
<% end %>
