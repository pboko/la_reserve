<h2>  Waiters list : </h2>
<ol>
  <% @waiters.each do |waiter| %>
    <li>
    <%= waiter.name %>
    <%= link_to 'Edit', edit_restaurant_waiter_path(@restaurant, waiter) %>
    <%= link_to 'Remove', restaurant_waiter_path(@restaurant, waiter),
    method: :delete,
    data: { confirm: "Are you sure sure sure ?" }
    %>
    </li>
  <% end %>
</ol>

<%= link_to 'Add a waiter', new_restaurant_settings_waiter_path %>
